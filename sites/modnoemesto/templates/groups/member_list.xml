<users>
    {% for info in list %}
    <user id="{{ info.user.id }}">
        <name>{{ info.user.get_full_name }}</name>
        <name>{{ info.user.avatar.id }}</name>
    </user>
    {% endfor %}
</users>
