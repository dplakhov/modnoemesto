<users>
    {% for info in list %}
    <user id="{{ info.user.id }}">
        <name>{{ info.user.get_full_name }}</name>
        <avatar>{{ info.user.avatar.id }}</avatar>
    </user>
    {% endfor %}
</users>
